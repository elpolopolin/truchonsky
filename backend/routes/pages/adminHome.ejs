<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Panel Admin</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
            background: #f0f0f0;
        }
        .container {
            text-align: center;
            background: white;
            padding: 40px;
            border-radius: 8px;
            box-shadow: 0 4px 8px rgba(0,0,0,0.1);
            max-width: 800px;
        }
        h1 {
            margin-bottom: 20px;
        }
        .btn {
            display: inline-block;
            padding: 15px 30px;
            margin: 10px;
            font-size: 16px;
            color: white;
            background: #007BFF;
            border: none;
            border-radius: 5px;
            cursor: pointer;
            text-decoration: none;
            transition: background 0.3s;
        }
        .btn-logout {
            background: #ff4d4d; /* Color rojo para el botón de logout */
        }
        .btn:hover {
            background: #0056b3;
        }
        .img {
            width: 100%;
        }
    </style>
</head>
<body>
    <div class="container">
        <img class="img" src=" https://skilled-obviously-earwig.ngrok-free.app/img/logo4.png" alt="Logo">
        <a href="/admin/productos" class="btn">Ver Productos</a>
        <a href="/admin/" class="btn">Ver Pedidos</a>
      
            <button class="btn btn-logout" type="button" onclick="deleteCookie('token')">Logout</button>
           
    </div>
    <script>
        // Función para eliminar una cookie por su nombre
        function deleteCookie(name) {
            document.cookie = name + "=; expires=Thu, 01 Jan 1970 00:00:00 UTC; path=/;";
        }
    </script>
</body>
</html>